# 剪贴板助手

一个基于 Python 和 PyQt6 开发的轻量 Windows 剪贴板历史工具。程序常驻系统托盘，在本地保存文本剪贴板历史，并支持搜索、复制、删除、导出和设置。

## 功能

- 监听其他应用程序复制的文本
- 最新记录始终显示在列表最上方
- 搜索剪贴板历史
- 双击记录即可复制原始文本
- 右键删除单条记录，或一键清空全部记录
- 导出历史记录为 UTF-8 文本文件
- 系统托盘常驻和单实例保护
- 自动过滤短时间内重复的剪贴板通知
- 可设置最大历史记录数和重复内容去重时间
- 可设置开机自动启动和启动后是否显示主窗口
- 可更换托盘图标

## 运行环境

- Windows 10 或更高版本
- Python 3.11 或更高版本

安装依赖：

```powershell
python -m pip install -r requirements.txt
```

## 运行程序

在项目目录中执行：

```powershell
python main.py
```

关闭主窗口后，程序会继续在系统托盘中运行。可通过托盘菜单重新打开窗口或彻底退出程序。

## 打包 EXE

安装 PyInstaller 后执行：

```powershell
python -m pip install pyinstaller
python -m PyInstaller --noconfirm --clean --onefile --windowed --name "ClipboardAssistant" --icon "1.ico" --add-data "1.ico;." main.py
```

生成的可执行文件位于 `dist/` 目录。

## 数据与隐私

剪贴板历史和设置保存在本机：

```text
%LOCALAPPDATA%\ClipboardAssistant\
```

程序只保存文本剪贴板内容。剪贴板历史可能包含密码、访问令牌或私密消息，因此本地数据库已通过 `.gitignore` 排除，不会提交到 Git 仓库。

## 项目结构

```text
core/   剪贴板监听和应用设置
db/     SQLite 历史记录存储
ui/     主窗口、设置窗口和托盘图标
main.py 程序入口
```
