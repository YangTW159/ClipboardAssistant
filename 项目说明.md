# ClipboardAssistant 剪贴板助手

一款基于 Python + PyQt6 开发的 Windows 平台剪贴板历史管理小工具。程序常驻系统托盘，在本地保存文本剪贴板历史，并支持搜索、复制、删除、导出和设置。

English version: [README.md](README.md)

## 功能

1. 实时全局监听系统剪贴板，自动保存其他应用程序复制的文本内容
2. 主窗口关闭后最小化到系统托盘后台常驻，右键菜单可彻底退出程序
3. 支持关键词搜索、清空全部记录和导出为 TXT 文档
4. 双击历史条目可快速重新复制原始文本到剪贴板
5. 右键历史条目可删除单条记录
6. 最新记录始终显示在列表最上方，并自动过滤短时间内的重复通知
7. 支持设置最大记录数、相同内容去重时间、开机自动启动、启动后显示窗口和自定义托盘图标
8. 单实例保护，避免重复启动产生多个监听器
9. 数据全部存储在本地 SQLite 数据库，无网络上传，隐私可控

## 界面预览

![剪贴板助手主界面](assets/main-window.png)

## 环境要求

- Windows 10 或更高版本
- Python 3.11 或更高版本

## 从 Release 直接下载

打开 [Releases 页面](https://github.com/YangTW159/ClipboardAssistant/releases/latest)，下载最新的 `ClipboardAssistant.exe` 直接运行即可，无需安装 Python。

> 首次运行时 Windows SmartScreen 可能提示未签名，选择“更多信息 → 仍要运行”。

## 从源码运行

安装依赖：

```powershell
python -m pip install -r requirements.txt
```

在项目目录中执行：

```powershell
python main.py
```

关闭主窗口后，程序会继续在系统托盘中运行。可通过托盘菜单重新打开窗口或彻底退出程序。

## 打包 EXE

```powershell
python -m pip install pyinstaller
python -m PyInstaller --noconfirm --clean --onefile --windowed `
  --name "ClipboardAssistant" --icon "1.ico" --add-data "1.ico;." main.py
```

生成的可执行文件位于 `dist/` 目录。

## 开发与测试

安装开发依赖并运行单元测试：

```powershell
python -m pip install -r requirements-dev.txt
python -m pytest -q
```

测试覆盖数据库层（插入 / 去重 / 数量上限 / 删除 / 导出）和设置层（默认值 / 保存读取 / 损坏 JSON 回退）。所有测试都使用临时目录，不会影响你真实的剪贴板数据库。

## 持续集成与发布

- **CI**（`.github/workflows/ci.yml`）：每次 push 和 Pull Request 时，在 Windows 上用 Python 3.11 和 3.12 自动运行测试。
- **Release**（`.github/workflows/release.yml`）：推送 `v*` 标签时，自动用 PyInstaller 构建 EXE 并上传到 GitHub Release。

发布新版本：

```powershell
git tag v1.0.0
git push origin v1.0.0
```

推送标签后工作流会自动构建 `ClipboardAssistant.exe` 并生成 Release。

## 数据与隐私

剪贴板历史和设置保存在本机：

```text
%LOCALAPPDATA%\ClipboardAssistant\
```

程序只保存文本剪贴板内容。剪贴板历史可能包含密码、访问令牌或私密消息，因此本地数据库和设置文件已通过 `.gitignore` 排除，不会提交到 Git 仓库。

建议避免复制密码、访问令牌等敏感信息；如有误存，可在主窗口中右键删除单条记录，或使用“清空全部记录”删除全部历史。

## 项目结构

```text
core/   剪贴板监听和应用设置
db/     SQLite 历史记录存储
ui/     主窗口、设置窗口和托盘图标
tests/  pytest 单元测试
main.py 程序入口
```

## 许可证

[MIT](LICENSE) © YangTW159
